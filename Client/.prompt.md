# Client Prompt Templates

Component scaffold

```
Create a Vue 3 Single File Component named `<Feature>.vue` under `src/modules/<Feature>/`.
Include a minimal template, props interface (TypeScript), and basic unit test skeleton using Vitest.
```
